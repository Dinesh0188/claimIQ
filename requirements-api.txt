# Slim dependency set for the FastAPI backend container (Dockerfile).
#
# This is requirements.txt minus streamlit + plotly, which the API never imports --
# they belong to the legacy ui/ Streamlit app only. Cutting them here matters because
# they pull in a large dependency tree that does nothing for a container that only
# ever serves claimiq.api:app, and free/starter container tiers are tight on both
# build time and image size.
#
# Keep this in sync with requirements.txt for everything except streamlit/plotly.

# --- core ---
pydantic>=2.7
python-dotenv>=1.0
openai>=1.40

# --- api ---
fastapi>=0.111
uvicorn[standard]>=0.30
pandas>=2.2
requests>=2.32

# --- retrieval (no torch: fastembed uses onnxruntime) ---
fastembed>=0.3
rank-bm25>=0.2.2
numpy>=1.26

# --- documents ---
pdfplumber>=0.11
pypdfium2>=4.30
reportlab>=4.2
rapidocr-onnxruntime>=1.3

# --- agent orchestration ---
langgraph>=0.2

# --- cache ---
diskcache>=5.6
