# --- core ---
pydantic>=2.7
python-dotenv>=1.0
openai>=1.40

# --- api + ui ---
fastapi>=0.111
uvicorn[standard]>=0.30
streamlit>=1.36
plotly>=5.22
pandas>=2.2
requests>=2.32

# --- retrieval (no torch: fastembed uses onnxruntime) ---
fastembed>=0.3
rank-bm25>=0.2.2
numpy>=1.26

# --- documents ---
pdfplumber>=0.11
pypdfium2>=4.30
reportlab>=4.2
# Local OCR for scanned bills. Runs on the onnxruntime fastembed already installs --
# no PyTorch, no Tesseract binary. Replaces the vision model on scans.
rapidocr-onnxruntime>=1.3

# --- agent orchestration ---
langgraph>=0.2

# --- cache ---
diskcache>=5.6

# --- dev ---
pytest>=8.2
ruff>=0.5
